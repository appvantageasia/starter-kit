import { Spin } from 'antd';
import styled from 'styled-components';

const LoadingElement = styled(Spin)`
    width: 100%;
    margin: 20px 0 !important;
    padding: 30px 50px !important;
    text-align: center;
`;

export default LoadingElement;
