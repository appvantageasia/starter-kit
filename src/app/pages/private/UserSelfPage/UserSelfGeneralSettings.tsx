import { Card } from 'antd';

const UserSelfGeneralSettings = () => (
    <Card size="small" title="Pushes open the window">
        and raises the spyglass.
    </Card>
);

export default UserSelfGeneralSettings;
